# Development placeholders — real secrets are delivered via /run/base44/app.env
# Clerk (required to boot — ClerkProvider + auth middleware)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_dev_placeholder
CLERK_SECRET_KEY=sk_test_dev_placeholder
# AWS S3 (only needed for PDF upload feature)
NEXT_PUBLIC_S3_BUCKET_NAME=dev-placeholder
NEXT_PUBLIC_S3_BUCKET_REGION=us-east-1
NEXT_PUBLIC_S3_ACCESS_KEY_ID=dev-placeholder
NEXT_PUBLIC_S3_SECRET_ACCESS_KEY_ID=dev-placeholder
# Pinecone (only needed for PDF embedding feature)
PINECONE_INDEX=dev-placeholder
# OpenAI (only needed for PDF embedding feature)
OPENAI_API_KEY=sk-dev-placeholder
